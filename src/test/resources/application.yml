spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:postgresql://localhost:5432/question_response
    username: postgres
    password: postgres
    platform: postgres

